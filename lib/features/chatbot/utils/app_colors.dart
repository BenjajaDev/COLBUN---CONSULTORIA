// ===========================================================================
// IMPORTACIONES
// ===========================================================================
import 'package:flutter/material.dart';

// ===========================================================================
// PALETA DE COLORES DE LA APLICACION
// ===========================================================================
/// Clase que define los colores utilizados en la aplicacion
/// Incluye paletas para tema claro y tema oscuro
class AppColors {
  // ===========================================================================
  // COLORES PARA EL TEMA CLARO
  // ===========================================================================
  static const Color lightprimary = Color(0xFF4D67AE);           // Azul principal para tema claro
  static const Color lightbackground = Colors.white;             // Fondo blanco para tema claro
  static const Color lightTextFieldBorder = Color(0xFFE0E0E0);   // Borde gris claro para campos de texto
  static const Color lightbackgroundBody = Color(0xFFFAFAFA);    // Fondo alternativo para cuerpo

  // ===========================================================================
  // COLORES PARA EL TEMA OSCURO
  // ===========================================================================
  static const Color darkprimary = Color(0xFF494C6B);            // Azul grisaceo para tema oscuro
  static const Color darkBackground = Color(0xFF252525);         // Fondo gris oscuro
}
